## What

<!-- One-line summary of what this changes. -->

## Why

<!-- Context: what bug does it fix, what feature does it add, what was wrong. -->

## Checklist

- [ ] `ruff check .` passes locally
- [ ] `mypy app` passes locally (zero errors)
- [ ] `pytest -q` passes locally
- [ ] New env vars documented in `.env.example` if added
- [ ] DB migrations added if schema changed
